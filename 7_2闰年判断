#define  _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
void rennain(int n) {
	if (n % 4 == 0 && n % 100 != 0 || n % 400 == 0)
		printf("yes");
	else
		printf("no");
}
int main() {
	int n;
	scanf("%d", &n);
	rennain(n);
	return 0;
}
